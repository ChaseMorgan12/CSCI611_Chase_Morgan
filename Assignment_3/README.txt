To execute the code just click the run all button.
Once everything is ran it should give all images in a folder named runs.
You will need to download the dataset and put it in a folder named 'dataset'